import { ScreenPlaceholder } from "@/screens/app/ScreenPlaceholder"

/** `/app/history` — streak & completion stats. */
function HistoryScreen() {
  return <ScreenPlaceholder title="기록" />
}

export { HistoryScreen }
